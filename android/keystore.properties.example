# Copy to keystore.properties for local release builds. Never commit the real file.
# CI supplies these via the KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD secrets instead.
storeFile=release-keystore.jks
storePassword=
keyAlias=emoji-guesser
keyPassword=
